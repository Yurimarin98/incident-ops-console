import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-consult-incidents',
  standalone: true,
  imports: [CommonModule, FormsModule],
  templateUrl: './consult-incidents.html',
  styleUrls: ['./consult-incidents.scss']
})
export class ConsultIncidents {
  
}
